# Screenshots & Diagrams

Place ComfyUI workflow screenshots, panel layout diagrams, and before/after examples in this folder.

Suggested files to add:

- `workflow-overview.png` — Full ComfyUI workflow with both Cropper and Assembler nodes.
- `debug-overlay-example.png` — Contact sheet with the debug overlay grid drawn on top.
- `panel-mapping-cheatsheet.png` — Visual grid showing cell indices 0-8 and default mapping.
- `installation-manager.png` — ComfyUI Manager "Install via Git URL" screenshot.

Once images are added, reference them from the main `README.md` using relative paths, e.g.:

```markdown
![Debug overlay example](docs/images/debug-overlay-example.png)
```
